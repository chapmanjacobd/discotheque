{
  "name": "discotheque-web",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "cover": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "jsdom": "^28.1.0",
    "vitest": "^4.0.18"
  }
}
