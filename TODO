Let's split the syncthing/syncweb stuff into a new repo that is focused on file management rather than being a media player

Let's port the xklb bookmarking and link related things to a new repo. No fts but search via fast golang filtering

## `web/` (Frontend)
- [ ] `app.js`: Refactor frontend logic into smaller modules or components.
- [ ] Implement better state management (e.g., using a lightweight store instead of global variables).
- [ ] Improve UI responsiveness and mobile experience.
- [ ] Increase test coverage for frontend components (currently 20+ `.test.js` files, but verify depth).
- [ ] Optimize loading performance for large media lists.

mountpoints list like Android File Browser and auto-unmount duplicate mountpoints on initial scan
